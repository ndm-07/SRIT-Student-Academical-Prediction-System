<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marks and Grade Prediction</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <h1>Marks and Grade Prediction</h1>
    
    <!-- Initial Choice Selection -->
    <form method="POST">
        {% csrf_token %}
        <label for="choice">Select an option:</label>
        <select id="choice" name="choice" required>
            <option value="1" {% if choice == 1 %}selected{% endif %}>Calculate Internal Marks of MID Exams</option>
            <option value="2" {% if choice == 2 %}selected{% endif %}>Calculate Total Internal Marks (MID + CAA)</option>
            <option value="3" {% if choice == 3 %}selected{% endif %}>Predict Grade Based on Internal + SEM Marks</option>
        </select><br><br>
        <button type="submit">Next</button>
    </form>

    <!-- Display the form based on the choice -->
    {% if choice == 1 %}
    <form method="POST">
        {% csrf_token %}
        <label for="mid1_marks">Enter Mid 1 marks:</label>
        <input type="number" id="mid1_marks" name="mid1_marks" required><br><br>

        <label for="mid2_marks">Enter Mid 2 marks:</label>
        <input type="number" id="mid2_marks" name="mid2_marks" required><br><br>

        <button type="submit" name="choice" value="1">Calculate Internal Marks</button>
        {% if internal_marks %}
            <h3>Calculated Internal Marks (MID Exams): {{ internal_marks }}</h3>
        {% endif %}
    </form>
    {% endif %}

    {% if choice == 2 %}
    <form method="POST">
        {% csrf_token %}
        <label for="mid1_marks">Enter Mid 1 marks:</label>
        <input type="number" id="mid1_marks" name="mid1_marks" required><br><br>

        <label for="mid2_marks">Enter Mid 2 marks:</label>
        <input type="number" id="mid2_marks" name="mid2_marks" required><br><br>

        <label for="caa1_marks">Enter CAA 1 marks:</label>
        <input type="number" id="caa1_marks" name="caa1_marks" required><br><br>

        <label for="caa2_marks">Enter CAA 2 marks:</label>
        <input type="number" id="caa2_marks" name="caa2_marks" required><br><br>

        <button type="submit" name="choice" value="2">Calculate Total Internal Marks</button>
        {% if total_internal_marks %}
            <h3>Calculated Total Internal Marks (MID + CAA): {{ total_internal_marks }}</h3>
        {% endif %}
    </form>
    {% endif %}

    {% if choice == 3 %}
    <form method="POST">
        {% csrf_token %}
        <label for="internal_marks_input">Enter Internal Marks:</label>
        <input type="number" id="internal_marks_input" name="internal_marks_input" required><br><br>

        <label for="sem_marks_input">Enter SEM Marks:</label>
        <input type="number" id="sem_marks_input" name="sem_marks_input" required><br><br>

        <button type="submit" name="choice" value="3">Predict Grade</button>
        {% if grade %}
            <h3>Your Predicted Grade: {{ grade }}</h3>
        {% endif %}
    </form>
    {% endif %}

    {% if error_message %}
        <h3 style="color: red;">{{ error_message }}</h3>
    {% endif %}
</body>
</html>
